
{/* Modal */ }
{/* <dialog id={`modal_${name}`} className="modal modal-bottom sm:modal-middle">
        <div className="modal-box bg-slate-900 text-white">
          <h3 className="font-bold text-lg mb-2">{name}</h3>
          <p className="py-2">{details}</p>
          <div className="flex flex-wrap gap-2 my-2">
            {techList.map((tech, idx) => (
              <span
                key={idx}
                className="badge badge-accent badge-outline rounded-lg px-2 py-1 text-sm"
              >
                {tech}
              </span>
            ))}
          </div>
          <div className="modal-action">
            <form method="dialog">
              <button className="btn">Close</button>
            </form>
          </div>
        </div>
      </dialog> */}